# MediaMTX config — LL-HLS sidecar
# We do NOT touch Flask/FFmpeg flow.
# This packager ingests RTSP directly from the camera and serves LL-HLS (with PARTs).

# === HLS/LL-HLS (HTTP) ===
hls: yes
hlsAddress: :8888
hlsVariant: lowLatency          # emit #EXT-X-PART + PRELOAD-HINT
hlsSegmentCount: 7              # keep very small live window beware of: "ERR [HLS] [muxer REOLINK_OFFICE] Low-Latency HLS requires at least 7 segments"
hlsSegmentDuration: 1000ms       # Instead of 1s - creates 7×0.5s = 3.5s buffer (200ms is the MINIMUM supported by MediaMTX)
hlsPartDuration: 500ms          # partial segment duration (ALLWAYS HALF OF hlsSegmentDuration)
hlsAllowOrigin: "*"             # allow browser clients
hlsAlwaysRemux: yes             # stable timing even if source timestamps wobble

# Make sure at least one ingest is enabled that matches the NVR publisher.
# If we publish RTMP from NVR:
rtmp: yes
rtmpAddress: :1935

# If we publish RTSP from NVR instead, ensure:
rtsp: yes
rtspAddress: :8554

paths:
  68d49398005cf203e400043f:
    source: publisher
  T8416P0023352DA9:
    source: publisher
  T8416P0023370398:
    source: publisher
  T8416P00233717CB:
    source: publisher
  T8416P0023390DE9:
    source: publisher
  T8416P6024350412:
    source: publisher
  T8419P0024110C6A:
    source: publisher
  T8441P12242302AC:
    source: publisher
  T8441P122428038A:
    source: publisher
  C6F0SgZ0N0PoL2:
    source: publisher
  95270001NT3KNA67:
    source: publisher
